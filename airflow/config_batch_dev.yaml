default_args:
  catchup: false
  delay: 2
  retries: 3
  schedule_interval: 0 7 30 * *
  starting_date: '20230215'

pipelines:
  dim-spark-campaigns:
    args:
      - execution_date: "{{ ds_nodash }}"
      - next_execution_datetime: "{{ next_execution_date }}"
      - mode: batch
      image: eu.gcr.io/tmg-data-preprod/clarity-dim-spark-campaigns-pipeline
      tag: d884563bcd35b6f8e01fad93485b26addf4d62d9

  dim-spark-campaigns-urls:
    args:
      - execution_date: "{{ ds_nodash }}"
      - next_execution_datetime: "{{ next_execution_date }}"
      - mode: batch
      image: eu.gcr.io/tmg-data-preprod/clarity-dim-spark-campaigns-urls-pipeline
      tag: d884563bcd35b6f8e01fad93485b26addf4d62d9

  dfp:
    args:
      - execution_date: "{{ ds_nodash }}"
      - next_execution_datetime: "{{ next_execution_date }}"
      - mode: batch
      image: eu.gcr.io/tmg-data-preprod/clarity-dfp-pipeline
      tag: d884563bcd35b6f8e01fad93485b26addf4d62d9

  competition-formstack:
    args:
      - execution_date: "{{ ds_nodash }}"
      - next_execution_datetime: "{{ next_execution_date }}"
      - mode: batch
      image: eu.gcr.io/tmg-data-preprod/up-source-pipeline
      tag: d884563bcd35b6f8e01fad93485b26addf4d62d9

  video-youtube:
    args:
      - execution_date: "{{ ds_nodash }}"
      - next_execution_datetime: "{{ next_execution_date }}"
      - mode: batch
      image: eu.gcr.io/tmg-data-preprod/up-source-pipeline
      tag: d884563bcd35b6f8e01fad93485b26addf4d62d9

  tcuk:
    args:
      - execution_date: "{{ ds_nodash }}"
      - next_execution_datetime: "{{ next_execution_date }}"
      - mode: batch
      image: eu.gcr.io/tmg-data-preprod/up-source-pipeline
      tag: d884563bcd35b6f8e01fad93485b26addf4d62d9

  article-tcuk:
    args:
      - execution_date: "{{ ds_nodash }}"
      - next_execution_datetime: "{{ next_execution_date }}"
      - mode: batch
      image: eu.gcr.io/tmg-data-preprod/up-source-pipeline
      tag: d884563bcd35b6f8e01fad93485b26addf4d62d9

  competition-allplatforms:
    args:
      - execution_date: "{{ ds_nodash }}"
      - next_execution_datetime: "{{ next_execution_date }}"
      - mode: batch
      image: eu.gcr.io/tmg-data-preprod/up-source-pipeline
      tag: d884563bcd35b6f8e01fad93485b26addf4d62d9

  applenews:
    args:
      - execution_date: "{{ ds_nodash }}"
      - next_execution_datetime: "{{ next_execution_date }}"
      - mode: batch
      image: eu.gcr.io/tmg-data-preprod/up-source-pipeline
      tag: d884563bcd35b6f8e01fad93485b26addf4d62d9

  article-applenews:
    args:
      - execution_date: "{{ ds_nodash }}"
      - next_execution_datetime: "{{ next_execution_date }}"
      - mode: batch
      image: eu.gcr.io/tmg-data-preprod/up-source-pipeline
      tag: d884563bcd35b6f8e01fad93485b26addf4d62d9

  article-allplatforms:
    args:
      - execution_date: "{{ ds_nodash }}"
      - next_execution_datetime: "{{ next_execution_date }}"
      - mode: batch
      image: eu.gcr.io/tmg-data-preprod/up-source-pipeline
      tag: d884563bcd35b6f8e01fad93485b26addf4d62d9